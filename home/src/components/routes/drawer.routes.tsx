import {createDrawerNavigator} from '@react-navigation/drawer';

const {Navigator} = createDrawerNavigator();

export default function DrawerRoutes(){
  return(
    <Navigator> </Navigator> //insira dentro desta tag as telas a ser mudadas
  );
}