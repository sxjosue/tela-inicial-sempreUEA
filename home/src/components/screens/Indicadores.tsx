import React from "react-native";
import { View } from "react-native";

export default function Indicadores_instucionais(){
    return(
        <View style={{flex:1, backgroundColor:'black'}}>
        </View>
    );
}